---
import Icon from "astro-icon";
import { getCollection } from "astro:content";
import ContentSection from "~/components/content-section.astro";
import Carousel from "./carousel/carousel.vue";
import type { Images } from "./carousel/carousel.vue";
import moonImage from "~/assets/moon.jpg";

const featuredSites = await getCollection(
  "showcase",
  ({ data }) => "featured" in data
);
const orderedSites = featuredSites.sort(
  (a, b) => a.data.featured! - b.data.featured!
);

const images: Images[] = [
  {
    path: moonImage.src,
    alt: 'moonasdasd',
    description: 'moon moon'
  },
  {
    path: moonImage.src,
    alt: 'mooasdn',
    description: 'moon moon'
  },
  {
    path: moonImage.src,
    alt: 'moonasd',
    description: 'moon moon'
  },
  {
    path: moonImage.src,
    alt: 'moonasd',
    description: 'moon moonasd'
  },
]
---

<ContentSection title="Hackmeetings Previos" id="showcase">
  <Fragment slot="lead">
    Revisa <span class="text-primary">nuestras memorias</span>. Más que solo un evento, 
    un kernel que late desde el 2010 en Bolivia
  </Fragment>

  <Carousel images={images} client:load/>

  <a
    href="https://memorias.hackmeeting.org.bo/?source=website"
    target="_blank"
    rel="noopener"
    class="flex items-center justify-center gap-3 border-2 border-current px-6 py-4 rounded-3xl"
  >
    <Icon pack="mdi" name="web" class="h-10" />
    <span>Nuestas memorias</span>
  </a>
</ContentSection>

<script>
  console.log('demo')

</script>